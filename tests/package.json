{
  "name": "claude-chat-tests",
  "version": "1.0.0",
  "description": "Test suite for ComfyUI Claude Chat extension",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "@vitest/ui": "^2.0.0",
    "jsdom": "^24.0.0",
    "happy-dom": "^14.0.0"
  }
}
